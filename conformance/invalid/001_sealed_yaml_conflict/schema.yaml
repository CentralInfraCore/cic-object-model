---
model: "0.1"
templates:
  $network-object:
    $.access.read:
      shape: object
      children:
        enabled:
          shape: scalar
          scalar_type: boolean
      content:
        enabled: false
root:
  shape: object
  children:
    access_policy:
      sealed_from:
        template: $network-object
        path: $.access.read
