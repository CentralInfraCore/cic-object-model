---
values:
  mtu:
    values: 9000
    origin: [values, origin]
    shape:
      values:
        type:
          values: scalar
          origin: [schema]
        scalar_type:
          values: integer
          origin: [schema]
      origin: [schema]
origin: [yaml]
