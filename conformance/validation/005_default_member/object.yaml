---
values:
  mtu:
    values: 1500
    origin: [schema]
    default: true
    shape:
      values:
        type:
          values: scalar
          origin: [schema]
        scalar_type:
          values: integer
          origin: [schema]
      origin: [schema]
origin: [yaml]
