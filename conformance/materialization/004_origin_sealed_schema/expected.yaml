---
values:
  access_policy:
    values:
      enabled:
        values: true
        origin: [{sealed: {template: $network-object, path: $.access.read}}, schema]
        shape:
          values:
            type:
              values: scalar
              origin: [schema]
            scalar_type:
              values: boolean
              origin: [schema]
          origin: [schema]
    origin: [{sealed: {template: $network-object, path: $.access.read}}]
    shape:
      values:
        type:
          values: object
          origin: [schema]
      origin: [schema]
origin: [yaml]
shape:
  values:
    type:
      values: object
      origin: [schema]
  origin: [schema]
