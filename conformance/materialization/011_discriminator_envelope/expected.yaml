---
values:
  mtu:
    values: 9000
    origin: [yaml]
    shape:
      values:
        type:
          values: scalar
          origin: [schema]
        scalar_type:
          values: integer
          origin: [schema]
      origin: [schema]
    access:
      values:
        modify:
          values:
            rules:
              values:
                network-admin:
                  values:
                    subjects:
                      values:
                      - OU=network-admins,O=acme
                      origin: [yaml]
                    effect:
                      values: allow
                      origin: [yaml]
                  origin: [yaml]
              origin: [yaml]
            inherit:
              values: true
              origin: [yaml]
          origin: [yaml]
      origin: [yaml]
origin: [yaml]
shape:
  values:
    type:
      values: object
      origin: [schema]
  origin: [schema]
