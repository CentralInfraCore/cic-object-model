---
values:
  interface:
    values:
      mtu:
        values: 9000
        origin: [yaml]
        shape:
          values:
            type:
              values: scalar
              origin: [schema]
            scalar_type:
              values: integer
              origin: [schema]
          origin: [schema]
      enabled:
        values: true
        origin: [schema]
        shape:
          values:
            type:
              values: scalar
              origin: [schema]
            scalar_type:
              values: boolean
              origin: [schema]
          origin: [schema]
    origin: [yaml]
    shape:
      values:
        type:
          values: object
          origin: [schema]
      origin: [schema]
origin: [yaml]
shape:
  values:
    type:
      values: object
      origin: [schema]
  origin: [schema]
