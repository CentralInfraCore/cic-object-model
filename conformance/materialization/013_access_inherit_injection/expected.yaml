---
values:
  mtu:
    values: 9000
    origin: [yaml]
    shape:
      values:
        type:
          values: scalar
          origin: [schema]
        scalar_type:
          values: integer
          origin: [schema]
      origin: [schema]
    role:
      values: config
      origin: [schema]
    access:
      values:
        read:
          values:
            rules:
              values:
                operator:
                  values:
                    subjects:
                      values:
                      - OU=operators,O=acme
                      origin: [schema]
                    effect:
                      values: allow
                      origin: [schema]
                  origin: [schema]
              origin: [schema]
            inherit:
              values: true
              origin: [schema]
            default_injection:
              values: 0
              origin: [schema]
          origin: [schema]
        modify:
          values:
            rules:
              values:
                network-admin:
                  values:
                    subjects:
                      values:
                      - OU=network-admins,O=acme
                      origin: [schema]
                    effect:
                      values: allow
                      origin: [schema]
                  origin: [schema]
              origin: [schema]
            inherit:
              values: 0
              origin: [schema]
          origin: [schema]
      origin: [schema]
origin: [yaml]
shape:
  values:
    type:
      values: object
      origin: [schema]
  origin: [schema]
