---
values:
  mtu:
    values: 1500
    origin: [schema]
    shape:
      values:
        type:
          values: scalar
          origin: [schema]
        scalar_type:
          values: integer
          origin: [schema]
      origin: [schema]
origin: [yaml]
shape:
  values:
    type:
      values: object
      origin: [schema]
  origin: [schema]
