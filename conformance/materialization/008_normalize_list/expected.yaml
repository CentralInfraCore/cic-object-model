---
cic:
  model: "0.1"
values:
  addresses:
    values:
      - values: 10.0.0.1/24
        origin: [yaml]
        shape:
          values:
            type: scalar
            scalar_type: string
          origin: [schema]
      - values: 10.0.0.2/24
        origin: [yaml]
        shape:
          values:
            type: scalar
            scalar_type: string
          origin: [schema]
    origin: [yaml]
    shape:
      values:
        type: list
      origin: [schema]
origin: [yaml]
